---
⚠️ AI-GENERATED DOCUMENT
Generated by: Claude (Sonnet 5)
Date: 4 August 2026 (originally), updated through 14 August 2026
Status: Describes a real, deployed, working system as of the latest update.
Contact: Steve Brown — steve@citational.ai
---

# CitationalAI — Query Pool (Phase 1: Pool Validation)

Rebuilt scaffold for the shared query pool. Product renamed Recce →
**CitationalAI** (domain: `citational.ai`). Infrastructure: Railway
(Postgres + Redis, queue + worker) — confirmed architecture, not
provisional.

## Why Railway, not Supabase + cron

The driver is the scan job model. A pool run hits three AI channels
(ChatGPT, Google AI Mode, Perplexity) per query, then runs an LLM
classification pass over each transcript. That's a queue-and-worker
problem (retries on failed channel calls, backpressure as volume grows,
visibility into what's mid-flight), not a fixed-schedule fire-and-forget
job. Cron has practical ceilings that bite here: 5-minute minimum
granularity, metered minutes, no persistent process. Redis backs a real job
queue, not a cache — the piece a pure cron model has no home for.

Railway won out over Vercel (function timeouts rule out long-running work)
and Fly.io (usage-billed, no hard ceiling) — Railway gives the same
worker/queue/Postgres/Redis model with hard spend caps.

**Status: confirmed.** Railway is the settled architecture for the scan
pipeline.

## Changelog note — brand-list redesign (v2)

Early real-world testing surfaced a design bug: `query_results` originally
stored a single `state` + `competitor_brand` pair, which only makes sense
relative to one specific brand. Phase 1 has no merchant yet to be that
brand, so the code silently defaulted to whichever brand happened to be
listed first in `brands.json` — which produced a confusing,
self-contradictory result. Fixed by replacing those two columns with
`recommended_brands` and `cited_brands` — the raw fact of which brands an
answer recommended vs. merely mentioned, with no target brand baked in at
collection time.

## Changelog note — mentions restructure (v3)

Charles's "Data Dependencies for Steve" doc asked for two things that
turned out to be the same underlying change: per-mention product name +
exact quote (`products[]`), and an explicit `mention_type` on competitor
mentions. Both come from the same classification pass over the same
transcript, so `recommended_brands`/`cited_brands` (two flat text arrays)
were replaced with a single `mentions` jsonb array — one object per brand
mention, carrying `brand`, `mention_type`, `product_name` (nullable), and
`quote` (nullable). Captured for every brand mentioned, not just the
merchant's own — no extra collection cost, and it resolves the doc's open
"hard boundary or not" question: whether a future API exposes competitor
product-level detail becomes a pure serving-layer decision, not something
the collection layer needs to pre-decide. Apply `npm run migrate:v3` once
against an existing v2 database — preserves existing data (backfills old
rows with `product_name`/`quote` = null) rather than dropping it.

## Changelog note — cost tracking fix

A cost-sizing query showed real numbers only for `google_ai_mode` and
nothing for `perplexity`/`chatgpt`. Diagnosis: `perplexity.js` had
`cost_usd` hardcoded to `null` even though Perplexity's own response
includes a real dollar figure at `usage.cost.total_cost` — simply never
read. Fixed. `chatgpt.js` genuinely can't report a reliable cost right now:
OpenAI's Chat Completions API doesn't return a dollar figure, only token
counts, and published per-model pricing is inconsistent across sources as
of Aug 2026. Rather than hardcode a possibly-wrong number, `cost_usd` stays
`null` there deliberately, with full token counts preserved in
`raw_response` for later computation once a specific rate is confirmed.

## What this is

- Postgres schema: `categories` → `subcategories` → `queries`, plus
  `shops`, `scans`, `query_results` (structured `mentions`),
  `subscriptions` (`db/schema.sql`)
- Starter seed: 60 queries across 3 categories / 6 subcategories
  (`db/seed.sql`)
- Three channel modules — Google AI Mode (DataForSEO), ChatGPT (OpenAI
  proxy), Perplexity — each returning `{ transcript, references,
  raw_response, cost_usd }` (`src/lib/channels/`)
- A classification pass using Claude to turn a transcript into a structured
  `mentions` list (`src/lib/classify.js`)
- A BullMQ queue + persistent worker (`src/worker.js`)
- A thin scheduler process (`src/scheduler.js`)
- A manual trigger script (`src/scripts/enqueuePoolRun.js`)
- Real search volume fetch (`src/scripts/fetchSearchVolume.js`)

## Channel caveats

- **Google AI Mode**: uses DataForSEO's `ai_mode/live/advanced` endpoint.
  Verified working against live data.
- **ChatGPT**: no public API for the ChatGPT consumer product itself — this
  calls the OpenAI Chat Completions API as a proxy. Treat as directional.
- **Perplexity**: genuinely browses and returns citations, closest analog
  to the consumer product.

## Classification caveat

`classify.js` is an LLM classification pass over free text (Claude), not
the NLP claim-extraction pipeline described in the SDX:VibeEvidence work.
Spot-check output against `query_results.raw_response` before trusting it
at scale — two real bugs (a self-contradictory target-brand default, and
confusing cited-source publishers with actual product brands) have already
been caught and fixed this way.

## Phase 1 vs Phase 2 — what `scans` means right now

No merchants yet. The scheduler creates one "pool scan" per category per
cadence tick with `shop_id = null`. From Phase 2 onward, a real
merchant-triggered scan gets its own row with `shop_id` set.

## Search volume

`queries.search_volume` is populated by a separate, periodic script:

```
railway ssh -s citational-pool -- npm run search-volume
```

Calls DataForSEO's Keyword Data API (`keywords_data/google_ads/search_volume/live`).
A single keyword with disallowed punctuation (parentheses, several other
symbols) fails the ENTIRE batched request — the script sanitizes each
keyword before sending, logging what changed. A `NULL` search_volume after
running this means DataForSEO measured no meaningful volume for that exact
phrase — a real answer, not a failure. Re-run this occasionally (e.g.
monthly) rather than on every deploy.

## Setup

1. **Railway**: create a project, add a **Postgres** service and a
   **Redis** service. In each other service that needs them, go to
   **Variables** and add a *reference variable* — type `DATABASE_URL` and
   use Railway's autocomplete (type `$`) to select
   `${{Postgres.DATABASE_URL}}`. Same for `REDIS_URL`. **Never wrap these
   in quotes** — `DATABASE_URL="${{Postgres.DATABASE_URL}}"` breaks the
   reference silently.
2. Add **two Railway services** from this repo: `npm run worker` and
   `npm run scheduler` as their respective start commands.
3. Copy `.env.example` to `.env` for local dev / set as Railway service
   variables: DataForSEO login/password, `OPENAI_API_KEY`,
   `PERPLEXITY_API_KEY`, `ANTHROPIC_API_KEY`.
4. `npm install`
5. Apply the schema: `npm run migrate` (Railway's Postgres has no built-in
   SQL editor).
6. Smoke test: `npm run enqueue:dry`
7. Small capped real run:
   ```
   npm run worker
   MAX_QUERIES_PER_RUN=5 npm run enqueue
   ```
8. Once that looks right: `npm run enqueue` for the full pool, let the
   scheduler take over from there.

If picking up an existing v1/v2 database, also run `npm run dedupe` (fixes
a historical duplicate-seed bug) and `npm run migrate:v3` (adds the
`mentions` structure) — both are safe to re-run.

## What's deliberately NOT built yet

- No merchant-facing anything (Shopify app, dashboard, billing) — `shops`
  and `subscriptions` exist in the schema but are empty.
- No competitor auto-discovery — `brands.json` is manually maintained
  reference data, no longer read by the collection pipeline itself.
- Classification is real but not exhaustively validated — spot-check
  before trusting at volume.
- No historical/time-series schema for league tables.
- Custom/merchant-submitted queries anticipated in the schema
  (`queries.source` includes `merchant_custom`) but not implemented.

## Next steps, roughly in order

1. Let the pool run for 8–12 weeks per the original frequency-stability
   validation goal.
2. Keep spot-checking `classify.js` output against real transcripts as
   volume grows.
3. Resolve the ChatGPT cost tracking question once OpenAI pricing for the
   model in use is confirmed against an actual billing statement.
4. Feed real `cost_usd` data into Charles's query-volume sizing work for
   Free/Essential/league tables.
